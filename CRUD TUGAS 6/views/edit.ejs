<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Data Pengguna</title>
</head>
<style>
    /* Reset default margin and padding */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Body styling */
body {
    font-family: 'Arial', sans-serif;
    background-color: #f7f7f7;
    padding: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
}

/* Header */
h2 {
    font-size: 2rem;
    margin-bottom: 20px;
    color: #333;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
}

/* Form Styling */
form {
    background-color: #fff;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
    width: 50%;
    animation: slideIn 1s ease;
}

form label {
    font-size: 1.1rem;
    color: #333;
}

form input {
    width: 100%;
    padding: 10px;
    margin: 10px 0;
    border: 1px solid #ddd;
    border-radius: 5px;
    font-size: 1rem;
}

form button {
    background-color: #ff8c00;
    color: white;
    border: none;
    padding: 10px 20px;
    border-radius: 5px;
    cursor: pointer;
    font-size: 1rem;
    transition: background-color 0.3s ease;
}

form button:hover {
    background-color: #ff7043;
}

/* Animations */
@keyframes slideIn {
    from {
        transform: translateY(50px);
        opacity: 0;
    }
    to {
        transform: translateY(0);
        opacity: 1;
    }
}

/* Responsive Design */
@media (max-width: 768px) {
    form {
        width: 80%;
    }

    h2 {
        font-size: 1.8rem;
    }
}

@media (max-width: 480px) {
    form input {
        padding: 8px;
    }

    form button {
        padding: 8px 16px;
    }
}

</style>
<body>
    <h2>Edit Data Pengguna Baru</h2>
    <form action="/update/<%= user.id  %>" method="post">
        <label for="Name">Nama:</label>
        <input type="text" id="name" name="name" required value="<%= user.name  %>"><br>
        <label for="Email">Email:</label>
        <input type="email" name="email" id="email" value="<%= user.email  %>"><br>
        <label for="phone">Telephone:</label>
        <input type="text" name="phone" id="phone" value="<%= user.phone  %>"><br>
        <button type="submit">Simpan</button>
    </form>
    <script>
        // Function to display alert after specific actions
        function showAlert(action) {
            if (action === 'input') {
                alert("Data pengguna baru berhasil ditambahkan!");
            } else if (action === 'update') {
                alert("Data pengguna berhasil diupdate!");
            } else if (action === 'delete') {
                alert("Data pengguna berhasil dihapus!");
            }
        }

        // Example trigger when form is submitted (update example)
        document.querySelector('form').addEventListener('submit', function(event) {
            event.preventDefault(); // Prevent form submission for demo
            showAlert('update'); // Call showAlert function after form submission
            this.submit(); // Submit the form after alert is shown
        });
    </script>
</body>
</html>